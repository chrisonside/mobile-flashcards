import {
  GET_DECK_DATA,
  ADD_DECK,
  ADD_CARD
} from '../actions';

function deckData (state = {}, action) {
  switch (action.type) {
    default :
      return state
  }
}

export default deckData;